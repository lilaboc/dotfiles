# ==========================================
#  完美复刻 Byobu 的 F 键宇宙
# ==========================================

# F2：新建窗口 (Window)
bind-key -n F2 new-window -c "#{pane_current_path}"

# F3 / F4：前后切换窗口
bind-key -n F3 previous-window
# 修复部分终端 F4 冲突，双保险绑定
bind-key -n F4 next-window     

# Shift + F2：水平分屏 (Pane)
bind-key -n S-F2 split-window -v -c "#{pane_current_path}"
# Ctrl + F2：垂直分屏 (Pane)
bind-key -n C-F2 split-window -h -c "#{pane_current_path}"

# Shift + 上下左右：在分屏(Pane)之间切换
bind-key -n S-Up select-pane -U
bind-key -n S-Down select-pane -D
bind-key -n S-Left select-pane -L
bind-key -n S-Right select-pane -R

# F6：安全断开连接 (Detach)
bind-key -n F6 detach-client

# 1. 完美复刻 F7：按下 F7 直接进入滚动查看模式（Tmux 的 Copy Mode）
bind-key -n F7 copy-mode

# ==========================================
#  完美复刻 F8：一键为当前窗口(Window)重命名
# ==========================================
bind-key -n F8 command-prompt -I "#W" "rename-window '%%'"

# 允许鼠标滚轮滚动（顺便解决你之前关心的滚动问题）
set -g mouse on

# 进去后可以用 j/k/u/d 导航，按 v 开始选择文本，按 y 复制（类似 Byobu 里的选择复制）
set-window-option -g mode-keys vi

# ==========================================
#  允许将标题动态同步到外层终端（Windows Terminal）
# ==========================================
set -g set-titles on

# 定义外层标签页标题的显示格式
# #H 表示当前服务器的主机名 (Hostname)
# #W 表示当前 Tmux 的窗口名 (比如你用 F8 改的名字)
set -g set-titles-string "#H : #W"
